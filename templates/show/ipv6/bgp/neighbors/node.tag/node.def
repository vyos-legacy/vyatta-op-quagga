help: Show detailed information on TCP and BGP neighbor connections for given address
allowed:
     l1=`ls /opt/vyatta/config/active/protocols/bgp/*/neighbor 2>/dev/null | egrep '^[0-9].*'`
     echo $l1
run: ${vyatta_bindir}/vtyshow.pl "show" "ip bgp neighbor $5"
